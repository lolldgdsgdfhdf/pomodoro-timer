# AGENTS Instructions

- This workspace contains a single simple Python script: [counter.py](counter.py).
- The project does not currently include tests, packaging, or build tooling.
- Keep changes minimal and focused on the script's existing behavior.
- When validating changes, run the script with:
  - `python counter.py`
- Prefer straightforward Python edits and avoid adding dependencies unless they are clearly necessary.
